# m4-qd-run-kind-verification-doc-sync Notes

This node aligns `docs/QD_WORKFLOW.md` with the committed qd roadmap checker vocabulary for `runs[].kind`.

The checker in `scripts/qd-report-check/roadmap.sh` accepts `implement`, `audit`, `ci`, `merge`, and `verification`.
The workflow documentation now lists the same vocabulary and explains that verification run records are produced by
`qd verification sign-off`.

No checker behavior changed. No historical evidence or report records were rewritten. This node makes no product
runtime, capture, encode, transport, decode, render, presentation, cross-device, latency, Mac hardware, workflow
dispatch, or credential-dependent claim.
